# =============================================================================
# DQLabs Airflow – Core Python Requirements
# Used by: webserver, scheduler, triggerer, worker, semantic-worker
# NOT included: ML/NLP stack (see requirements-ml.txt)
# NOT included: dqlabs connector wheels (see requirements-connectors.txt)
#
# VERSION POLICY:
#   Packages that also appear in the Airflow 2.8.1 constraints file are pinned
#   to the CONSTRAINT version (not the original MWAA version). This avoids
#   pip backtracking. The constraints file is applied in the Dockerfile.
# =============================================================================

# ---------------------------------------------------------------------------
# Airflow core — already installed in the base image (apache/airflow:2.8.1-python3.11)
# DO NOT re-add apache-airflow==2.8.1 here — it forces full re-resolution.
# Providers are installed below; the constraints file in the Dockerfile
# (constraints-2.8.1/constraints-3.11.txt) prevents backtracking.
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Airflow Providers
# ---------------------------------------------------------------------------
apache-airflow-providers-amazon==8.16.0
apache-airflow-providers-oracle==3.9.1
apache-airflow-providers-mysql==5.5.1
apache-airflow-providers-google==10.13.1
google-cloud-storage==2.14.0
apache-airflow-providers-postgres==5.10.0
apache-airflow-providers-ssh==3.10.0
apache-airflow-providers-mongo==3.5.0
apache-airflow-providers-tableau==4.4.0
apache-airflow-providers-odbc==4.4.0
apache-airflow-providers-databricks==6.0.0
apache-airflow-providers-apache-hive==6.4.1

# ---------------------------------------------------------------------------
# Celery + broker (aligned to constraints-2.8.1 versions)
# ---------------------------------------------------------------------------
celery==5.3.6
kombu==5.3.5
tornado==6.4

# ---------------------------------------------------------------------------
# Database
# ---------------------------------------------------------------------------
SQLAlchemy==1.4.51
psycopg2-binary==2.9.9
werkzeug==2.2.3
wtforms==3.1.2
redshift-connector==2.0.918
pyathena==3.5.1
oracledb==2.0.1
ibm_db==3.2.3

# ---------------------------------------------------------------------------
# Data processing (numpy/pandas aligned to constraints-2.8.1)
# ---------------------------------------------------------------------------
statsmodels==0.14.2
Cython==0.29.35
numpy==1.24.4
pandas==2.1.4
scikit-learn==1.5.2
pmdarima==2.0.3
prophet==1.1.5
cmdstanpy==1.1.0
python-dateutil==2.8.2
tqdm==4.66.1

# ---------------------------------------------------------------------------
# Cloud SDKs (azure-storage-blob aligned to constraints-2.8.1)
# ---------------------------------------------------------------------------
boto3==1.33.13
awscrt==0.27.6
azure-storage-blob==12.19.0
azure-identity==1.15.0
azure-mgmt-datafactory==4.0.0
azure-kusto-data==4.3.1
azure-storage-file-datalake==12.14.0

# ---------------------------------------------------------------------------
# SQL tooling (sqlparse aligned to constraints-2.8.1)
# ---------------------------------------------------------------------------
SQLGlot==26.29.0
sql_metadata==2.11.0
pandasql==0.7.3
sqlparse==0.4.4
pyhive==0.7.0
thrift-sasl==0.4.3

# ---------------------------------------------------------------------------
# ODBC / JDBC (pyodbc aligned to constraints-2.8.1)
# ---------------------------------------------------------------------------
pyodbc==5.0.1

# ---------------------------------------------------------------------------
# Notifications
# ---------------------------------------------------------------------------
slack==0.0.2
slackclient==2.9.4
aiohttp==3.9.1
pymsteams==0.2.2
sendgrid==6.11.0

# ---------------------------------------------------------------------------
# Reporting / PDF generation
# ---------------------------------------------------------------------------
xhtml2pdf==0.2.15
selenium
python-bidi==0.4.2

# ---------------------------------------------------------------------------
# Cryptography (aligned to constraints-2.8.1 — do NOT pin cryptography
# independently; gcloud-aio-auth transitive dep controls the upper bound)
# ---------------------------------------------------------------------------
pycryptodome==3.20.0
pycryptodomex==3.20.0

# ---------------------------------------------------------------------------
# Utilities (python-dotenv / ijson aligned to constraints-2.8.1)
# ---------------------------------------------------------------------------
python-dotenv==1.0.0
markupsafe==2.1.3
detect-delimiter==0.1.1
crccheck==1.3.1
google-crc32c==1.5.0
ijson==3.2.3
pip-system-certs==4.0
typing_extensions==4.9.0
